(function($, Drupal) {
  Drupal.APSJobsBlocksAgency = Drupal.APSJobsBlocksAgency || {};
  Drupal.behaviors.actionAPSJobsBlocksAgency = {
    attach: function(context) {
      $(window).load(function() {
      });
    }
  }

})(jQuery, Drupal);
